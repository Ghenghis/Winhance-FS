<UserControl x:Class="Winhance.WPF.Features.FileManager.Controls.BreadcrumbBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Winhance.WPF.Features.FileManager.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=viewModels:BreadcrumbBarViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Features/Common/Resources/Styles/TableViewStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="BreadcrumbButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Margin" Value="0,0,2,0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource ButtonPressedBackground}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <Style x:Key="BreadcrumbSeparatorStyle" TargetType="TextBlock">
                <Setter Property="Text" Value="/"/>
                <Setter Property="Margin" Value="4,0"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Foreground" Value="{DynamicResource SubTextColor}"/>
            </Style>
            
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{DynamicResource CardBackground}" 
            CornerRadius="4" 
            BorderThickness="1" 
            BorderBrush="{DynamicResource BorderBrush}"
            Padding="8,4">
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Drive Icon -->
            <iconPacks:PackIconMaterial Grid.Column="0" Kind="{Binding DriveIcon}" 
                                       Width="20" Height="20" 
                                       VerticalAlignment="Center" 
                                       Foreground="{DynamicResource PrimaryTextColor}"/>
            
            <!-- Breadcrumb Path -->
            <ItemsControl Grid.Column="1" Margin="8,0,0,0"
                          ItemsSource="{Binding Segments}"
                          Visibility="{Binding IsEditing, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource BreadcrumbSeparatorStyle}" 
                                       Visibility="{Binding IsFirst, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                            <Button Style="{StaticResource BreadcrumbButtonStyle}"
                                    Content="{Binding Name}"
                                    Command="{Binding DataContext.NavigateToSegmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="{Binding FullPath}">
                                <Button.ContextMenu>
                                    <ContextMenu ItemsSource="{Binding SubFolders}">
                                        <ContextMenu.ItemTemplate>
                                            <DataTemplate>
                                                <MenuItem Header="{Binding Name}" 
                                                         Command="{Binding DataContext.NavigateToPathCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                         CommandParameter="{Binding FullPath}"/>
                                            </DataTemplate>
                                        </ContextMenu.ItemTemplate>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <!-- Edit Mode -->
            <Grid Grid.Column="1" Margin="8,0,0,0"
                  Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0" 
                         Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         KeyDown="PathTextBox_KeyDown"
                         Style="{DynamicResource SearchTextBoxStyle}"/>
                
                <Button Grid.Column="1" 
                        Width="24" Height="24" 
                        Command="{Binding AcceptEditCommand}"
                        Style="{DynamicResource IconButtonStyle}"
                        ToolTip="Accept"
                        Margin="4,0,0,0">
                    <iconPacks:PackIconMaterial Kind="Check" Width="14" Height="14"/>
                </Button>
            </Grid>
            
            <!-- Recent Dropdown -->
            <Button Grid.Column="2" 
                    Width="28" Height="28" 
                    Command="{Binding ShowRecentCommand}"
                    Style="{DynamicResource IconButtonStyle}"
                    ToolTip="Recent Locations"
                    Margin="8,0,0,0">
                <iconPacks:PackIconMaterial Kind="History" Width="14" Height="14"/>
            </Button>
            
            <!-- Copy Path Button -->
            <Button Grid.Column="3" 
                    Width="28" Height="28" 
                    Command="{Binding CopyPathCommand}"
                    Style="{DynamicResource IconButtonStyle}"
                    ToolTip="Copy Path"
                    Margin="2,0,0,0">
                <iconPacks:PackIconMaterial Kind="ContentCopy" Width="14" Height="14"/>
            </Button>
            
            <!-- Edit Button -->
            <Button Grid.Column="4" 
                    Width="28" Height="28" 
                    Command="{Binding StartEditCommand}"
                    Style="{DynamicResource IconButtonStyle}"
                    ToolTip="Edit Path"
                    Margin="2,0,0,0">
                <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14"/>
            </Button>
            
        </Grid>
        
    </Border>
    
</UserControl>
